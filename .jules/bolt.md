## 2026-03-15 - [Nodes Refactoring to Async Operations]
**Learning:** For maintaining an AI agent-friendly setup, refactoring logic directly using manual scripting and matching via RegExp/index insertions needs careful consideration to not miss edge cases or break types (like `projectPath: string | null | undefined`).
**Action:** When creating async methods replacing synchronous equivalents inside a `switch-case` block within Godot MCP server tools (e.g., in `src/tools/composite/nodes.ts`), be sure to update method signatures, verify types (`projectPath: string | null | undefined`), run formaters/linters natively (`npm run check` or `biome check`), and re-run all test suites (`npm run test`) to ensure everything is strictly correct according to Biome schema before concluding.
